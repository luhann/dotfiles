#? Config file for btop v.1.4.7

# Only non-default overrides are listed here; btop fills in the rest
# from its internal defaults on first run and saves the full file on exit.

color_theme = "patroclus"
theme_background = false
update_ms = 500
clock_format = "%X /uptime"
swap_disk = false
shown_gpus = "nvidia amd intel"