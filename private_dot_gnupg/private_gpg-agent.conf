# Serve SSH authentication from the YubiKey's [A] subkey (2E010883268BB156)
# so no private SSH key has to exist on disk. Keys offered over this socket are
# listed in ./sshcontrol.
enable-ssh-support
